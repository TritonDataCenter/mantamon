{
    "name": "shrimp-zfs-hang-OS-2313",
    "global": true,
    "type": "cmd",
    "config": {
        "cmd": "test $(svcs -H -oFMRI -p marlin-agent | grep zfs | tr -s ' ' | cut -d ' '  -f 3 | xargs -n 1 ps -oetime -p | grep -v ELAPSED | tr -d ' ' | tr '[-:]' ' ' | awk '{ if (NF == 3) print 0 + $1; else if (NF == 4) print ($1 * 24) + $2; else print 0; } END { print 0; }' | sort -n -r | head -1) -lt 1",
        "threshold": "1",
        "period": "300"
    }
}
